---
phase: 08-bug-reporting
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - app/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Failures show a clear error with Retry while preserving the user's inputs"
  artifacts:
    - path: "app/page.tsx"
      provides: "Bug report error state with explicit Retry CTA"
      contains: "Retry"
  key_links:
    - from: "Bug report error state UI"
      to: "Bug report submit handler"
      via: "Retry button onClick"
      pattern: "Retry.*onClick|onClick.*Retry"
---

<objective>
Add an explicit Retry action to the bug report error state so users can resubmit without losing inputs.

Purpose: Close the verification gap for failure handling and make the retry path obvious.
Output: Updated bug report error UI in the Settings screen with a clear Retry CTA.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bug-reporting/08-CONTEXT.md
@.planning/phases/08-bug-reporting/08-bug-reporting-VERIFICATION.md
@.planning/phases/08-bug-reporting/08-01-SUMMARY.md
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit Retry CTA in bug report error state</name>
  <files>app/page.tsx</files>
  <action>
  1. Locate the bug report error state in `app/page.tsx` (the UI that renders when submission fails).
  2. Add a clearly labeled Retry action (button or link-style CTA) adjacent to the error message.
  3. Wire the Retry action to the existing submit handler so it reuses the current description + selected photo state (do not clear or reset inputs).
  4. Disable the Retry action while a submit is in progress to prevent duplicate submissions.
  5. Keep styling consistent with the existing Settings / bug report panel actions (spacing, hover states, and typography).
  </action>
  <verify>
  - Trigger a submission failure and confirm the error state shows a visible "Retry" action.
  - Click Retry and confirm it re-submits using the current description/photo without clearing inputs.
  </verify>
  <done>Bug report failures display an explicit Retry CTA that resubmits without losing user inputs.</done>
</task>

</tasks>

<verification>
- [ ] Error state shows a visible Retry action
- [ ] Retry uses existing inputs and does not clear the form
- [ ] Retry is disabled while submitting
</verification>

<success_criteria>
1. A failed bug report submission presents a clear Retry option.
2. Retrying uses the existing description and photo without data loss.
</success_criteria>

<output>
After completion, create `.planning/phases/08-bug-reporting/08-03-SUMMARY.md`
</output>
